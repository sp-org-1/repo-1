# Workflow to review commit or PR using GenAI agent
name: Review-Comment-PR
on:
  push:
    branches:
      - 'feature/**'
      - 'hotfix/**'
      - 'bugfix/**'
  pull_request:
    branches:
      - 'master'
      - 'develop'
      - 'releases/**'
  workflow_dispatch:
permissions: write-all
jobs:
  invoke-agent:
    runs-on: ubuntu-latest
    #runs-on: 
      #- codebuild-github-runner-01-${{ github.run_id }}-${{ github.run_attempt }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2
      - name: Get change type
        id: get-change-type
        env:
          GITHUB_CONTEXT: ${{ toJSON(github) }}
        run: echo "${{ GITHUB_CONTEXT }}"
      #- name: Invoke Agent
      #  uses: sp-org-1/invoke-aws-bedrock-agent@c61363fc98a7d040d4b0e26b40eae0a813585f23
      #  with:
      #    change-type: 'PR'
