name: Caller

on:
  workflow_dispatch:

jobs:
  job1:
    uses: sp-org-1/repo-1/.github/workflows/reusable.yml@master

  job2:
    runs-on: ubuntu-latest
    needs: job1
    steps:
      - run: echo ${{ needs.job1.outputs.firstword }} ${{ needs.job1.outputs.secondword }}
