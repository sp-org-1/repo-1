name: Windows-Variable-List-API
on:
  workflow_dispatch:
permissions:
  contents: read
  actions: read
  checks: write
jobs:
  job1:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build
        shell: pwsh
        run: |
          .\scripts\test.ps1 -accessToken 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 -orgaName sp-org-1
